class Solution:
	def pushZerosToEnd(self,arr):
    	# code here
        element = len(arr)
        j = 0
        for i in range(element):
            if arr[i] != 0:
                arr[j], arr[i] = arr[i], arr[j]
                j = j + 1
            
