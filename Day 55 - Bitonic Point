class Solution:

	def findMaximum(self, arr):
		# code here
		l = len(arr)
		n = arr[0]
		for i in range(1, l):
		    if arr[i] > n:
                n = arr[i]
            else:
                break
        return n
